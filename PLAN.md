# ADS-B Analytics MVP 実装計画

## 1. 目的

Linux x86-64 サーバー上で稼働中の readsb から ADS-B 復号データを安全に取得し、履歴を蓄積・分析できる個人向け Web アプリを構築する。

今回の作業範囲は次の2段階とする。

1. **Phase 0：環境テスト**
   - サーバー、readsb、ネットワーク、ディスク、実行基盤を読み取り専用で検査する。
   - 検査結果と合格・警告・不合格をレポート化する。
2. **Phase 1：MVP**
   - readsb の `aircraft.json` を定期取得して保存する。
   - 現在の航空機、24時間の交通量、最近の航跡、最遠・最接近ランキングを Web 画面に表示する。
   - Linux サーバーへコンテナとして配置し、再起動後も自動復旧することを確認する。

既存の readsb、tar1090、fr24feed の設定や通信経路は、明示的な必要性と承認がない限り変更しない。

---

## 2. MVPの完成像

ブラウザーでアプリを開くと、少なくとも以下を確認できる。

- 現在受信中の航空機数
- 過去24時間の「同時受信機数」と「ユニーク機体数」
- 過去24時間の航空機数の時系列グラフ
- 最近の航跡を表示する地図
- 受信地点からの最遠受信ランキング
- 受信地点への最接近ランキング
- 最終取得時刻とデータ取得状態
- readsbへの接続異常、データの停止、DB異常の表示

この段階では tar1090 の代替は目指さない。リアルタイム航空機追跡ではなく、履歴の保存と分析の基盤を作る。

---

## 3. 前提と既定値

情報が未指定の場合、以下を仮定する。Phase 0で実環境を確認し、異なる場合は設定だけを調整する。

| 項目 | 既定値 |
|---|---|
| CPU | x86-64 / `x86_64` |
| OS | systemdを使用する一般的なLinux |
| 配置方式 | Docker Engine + Docker Compose v2 |
| readsb取得元 | 同一サーバー上のHTTP JSONエンドポイント |
| 主データ | readsb `aircraft.json` |
| 取得間隔 | 5秒 |
| 航跡の保存間隔 | 同一機体につき原則30秒、または大きな位置変化時 |
| DB | PostgreSQL |
| API | Python + FastAPI |
| Web UI | 軽量なHTML/JavaScript、Apache ECharts、MapLibre GL JS |
| 公開範囲 | 初期状態ではLANまたはlocalhostのみ |
| アプリのタイムゾーン | サーバー設定を確認し、表示用タイムゾーンを設定可能にする |
| DB内の時刻 | UTC |
| 詳細位置データ保持 | 30日 |
| 1分集計データ保持 | 1年以上 |
| ログ保持 | ローテーションして14日程度 |

PostgreSQLを選ぶ理由は、長期間の位置履歴と集計クエリに余裕を持たせるためである。Phase 1ではPostGISやTimescaleDBを必須にせず、必要になった時点で追加できる構造にする。

---

## 4. Codexエージェント向け作業原則

- 作業開始時に本ファイルを読み、各タスクの状態を更新する。
- サーバー上の既存サービスを停止・再起動・再設定しない。
- Phase 0は原則として読み取り専用で実行する。
- `sudo`を必要とする変更、ファイアウォール変更、公開ポート追加、DNS/TLS設定は、対象と影響を提示してから実行する。
- 秘密情報をGit、ログ、画面、テスト成果物へ保存しない。
- `.env`はコミットせず、`.env.example`のみを管理する。
- readsbが返す実データをテスト用fixtureへ保存する場合、ICAOアドレス、callsign、座標などを匿名化する。
- 既存ポートとコンテナ名を調査してから、新しい名前とポートを決める。
- 外部公開はPhase 1の必須条件に含めない。まずローカル/LANで正常動作させる。
- 実装後は自動テスト、コンテナヘルスチェック、再起動試験、データ取得停止試験を行う。
- 想定外の環境を発見した場合、既存システムを変更して合わせるのではなく、アプリ側を適応させる。

---

## 5. Phase 0：環境テスト

### 5.1 目的

実装前に、MVPを安全に稼働させられるかを確認する。テスト結果は `reports/environment-report.md` と機械可読な `reports/environment-report.json` に残す。

### 5.2 テスト用成果物

リポジトリ内に次を用意する。

- `scripts/check_environment.sh`
  - サーバー情報、実行基盤、ポート、readsb接続を検査する。
  - 通常ユーザーで実行でき、システムを変更しない。
- `scripts/probe_readsb.py`
  - readsb JSONを取得し、HTTP状態、JSON形式、主要フィールド、更新状況を確認する。
  - 認証情報や全航空機データをレポートへ出力しない。
- `reports/environment-report.md`
  - 人間向けの結果、警告、推奨対応。
- `reports/environment-report.json`
  - 後続処理で利用できる検査結果。

### 5.3 検査項目

#### A. OSとCPU

- `uname -m`が`x86_64`である。
- ディストリビューション名とバージョンを記録する。
- カーネル、systemdの有無、ホスト名を記録する。
- CPUコア数を記録する。

#### B. メモリとディスク

- 総メモリと利用可能メモリを確認する。
- 配置先ファイルシステムの空き容量、ファイルシステム種別を確認する。
- inodeの空きを確認する。
- 合格目安：
  - 利用可能メモリ：1 GiB以上
  - 空きディスク：10 GiB以上
- 推奨目安：
  - メモリ：2 GiB以上
  - 空きディスク：30 GiB以上

閾値未満でも即座に変更せず、保存間隔・保持期間・DB構成の代替案をレポートする。

#### C. コンテナ実行基盤

- Docker Engineの有無とバージョンを確認する。
- `docker compose version`が利用可能か確認する。
- 実行ユーザーがDockerを利用できるか確認する。
- Dockerデータ領域の空き容量を確認する。
- 既存コンテナ、ネットワーク、使用中のコンテナ名を読み取り専用で確認する。
- Dockerがない場合は、不合格ではなく「要選択」とし、以下を提示する。
  - Dockerを導入する案
  - systemd + Python仮想環境 + PostgreSQLを使う案

#### D. readsb接続

- 候補URLを順に確認する。実URLが指定済みなら、それだけを使用する。
- HTTP接続、応答時間、HTTPステータスを確認する。
- JSONとして解析できることを確認する。
- `now`、`messages`、`aircraft`など、利用可能なトップレベルフィールドを記録する。
- 航空機要素から以下の利用可否を集計する。
  - `hex`
  - `flight`
  - `lat` / `lon`
  - `alt_baro` / `alt_geom`
  - `gs`
  - `track`
  - `baro_rate` / `geom_rate`
  - `seen`
  - `seen_pos`
  - `rssi`
- 10～15秒空けた2回の取得で、`now`または`messages`が進むことを確認する。
- readsbが一時的に無応答でも、既存サービスには触れずエラーとして記録する。

候補例は環境に合わせて設定する。

```text
http://127.0.0.1/tar1090/data/aircraft.json
http://127.0.0.1/readsb/data/aircraft.json
http://127.0.0.1/dump1090-fa/data/aircraft.json
```

#### E. 受信地点

- readsbの設定、`receiver.json`、既存設定ファイル、またはユーザー指定値から受信地点の緯度・経度を確認する。
- 取得できなければPhase 1開始前の必須入力とする。
- レポートには緯度・経度を小数点以下2桁程度へ丸めて記載し、公開用ログに精密な自宅座標を残さない。

#### F. 時刻

- サーバー時刻、タイムゾーン、UTCとの差を記録する。
- NTP同期状態を確認する。
- readsb JSONの`now`とサーバー時刻の差を測定する。
- 60秒を超える差は警告、5分を超える差はPhase 1のブロッカーとする。

#### G. ネットワークとポート

- 待ち受け中のTCPポートを読み取り専用で確認する。
- MVP候補ポートが未使用か確認する。
- localhostからreadsbへ接続できるか確認する。
- MapLibre用の外部地図タイルを使う場合、サーバーおよびブラウザーからの外部通信要否を記録する。
- ファイアウォールを変更しない。

#### H. 既存サービスへの影響

- readsb、tar1090、fr24feedが現在稼働中か確認する。
- サービス名、コンテナ名、公開ポートだけを記録する。
- 設定内容やfr24feedの共有キーなどの秘密情報は収集しない。
- Phase 0実行前後で主要サービスの状態が変わっていないことを確認する。

### 5.4 判定

各項目を以下で分類する。

- **PASS**：Phase 1をそのまま開始できる。
- **WARN**：開始できるが、設定変更または制限が推奨される。
- **FAIL**：Phase 1の安全な実行に必要な条件を満たさない。
- **UNKNOWN**：権限または情報不足で判定できない。

Phase 1へ進む最低条件：

- CPUがx86-64である。
- readsb JSONへ安定して接続できる。
- 受信地点を設定できる。
- 保存先に十分な空き容量がある、または保持期間を短縮する。
- 採用した実行方式を使用できる。
- 既存サービスとのポート衝突がない。
- 時刻の大きなずれがない。

FAILがある場合は勝手に修正せず、原因、影響、最小の修正案を提示して停止する。WARNだけの場合は、安全な設定へ調整してPhase 1へ進む。

### 5.5 Phase 0受け入れ条件

- [ ] 環境検査を通常ユーザーで実行できる。
- [ ] 検査によってファイル、パッケージ、サービス設定が変更されない。
- [ ] `environment-report.md`と`environment-report.json`が生成される。
- [ ] readsb JSONを2回以上取得し、更新されていることを確認できる。
- [ ] 既存3サービスの状態が検査前後で変わらない。
- [ ] Phase 1へ進めるかが明確に判定される。

---

## 6. Phase 1：MVP

### 6.1 システム構成

```text
readsb aircraft.json
        |
        v
 collector ─────> PostgreSQL
                       |
                       v
                 FastAPI / API
                       |
                       v
                   Web UI
```

Docker Composeでは原則として以下の3サービスに分ける。

- `adsb-collector`：readsb取得と保存
- `adsb-api`：API、画面配信、ヘルスチェック
- `adsb-db`：PostgreSQL

小規模サーバーではcollectorとAPIを同じイメージから起動してよいが、プロセスと責務は分離する。

### 6.2 ディレクトリ案

```text
.
├── Plan.md
├── README.md
├── compose.yaml
├── .env.example
├── app/
│   ├── api/
│   ├── collector/
│   ├── domain/
│   ├── static/
│   └── templates/
├── migrations/
├── scripts/
│   ├── check_environment.sh
│   ├── probe_readsb.py
│   ├── backup.sh
│   └── restore_test.sh
├── tests/
│   ├── fixtures/
│   ├── unit/
│   └── integration/
└── reports/
```

### 6.3 設定

最低限、環境変数で以下を設定できるようにする。

```text
READSB_AIRCRAFT_URL=
RECEIVER_LAT=
RECEIVER_LON=
DISPLAY_TIMEZONE=Asia/Tokyo
POLL_INTERVAL_SECONDS=5
TRACK_SAMPLE_SECONDS=30
RAW_RETENTION_DAYS=30
DATABASE_URL=
APP_BIND_HOST=127.0.0.1
APP_PORT=8088
MAP_STYLE_URL=
```

値の検証を起動時に行い、URLや座標が不正なら明確なメッセージを出して停止する。DBパスワードなどの秘密情報を画面や通常ログへ出さない。

### 6.4 データ取得仕様

- readsbの`aircraft.json`を既定5秒間隔で取得する。
- HTTPタイムアウトを短く設定し、失敗時に既存サービスへ負荷をかける連続再試行をしない。
- 指数バックオフを使い、回復後は通常周期へ戻す。
- `hex`を機体識別子として正規化する。
- `seen`や`seen_pos`が古いデータは「現在受信中」から除外する。
- 緯度・経度がない観測も、現在機数や受信状態の集計に利用できるようにする。
- 位置履歴は毎回保存せず、同一機体につき30秒ごと、または一定以上の移動・高度変化がある場合に保存する。
- 生のHTTPレスポンス全体は通常保存しない。
- DB停止中もメモリを無制限に消費しない。
- 同じ観測を再処理しても重複しにくい冪等性を持たせる。

### 6.5 最小データモデル

#### `aircraft`

- `icao`：主キー
- `first_seen_at`
- `last_seen_at`
- `last_callsign`

#### `observations`

- `id`
- `observed_at`
- `icao`
- `callsign`
- `lat`
- `lon`
- `altitude_ft`
- `ground_speed_kt`
- `track_deg`
- `vertical_rate_fpm`
- `rssi`
- `distance_km`
- `bearing_deg`
- `source_age_seconds`

主要インデックス：

- `(icao, observed_at)`
- `(observed_at)`
- 距離ランキング用の`(distance_km, observed_at)`

#### `traffic_minute`

- `bucket_at`
- `active_aircraft_count`
- `position_aircraft_count`
- `message_count_delta`

#### `ingestion_status`

- `checked_at`
- `success`
- `latency_ms`
- `aircraft_count`
- `error_code`

エラーメッセージへレスポンス本文や秘密情報を保存しない。

### 6.6 集計の定義

表示値の意味を曖昧にしない。

- **現在受信中**：最新取得データで`seen <= 15秒`のユニークICAO数。
- **位置取得中**：現在受信中かつ有効な緯度・経度があり、`seen_pos <= 30秒`のユニークICAO数。
- **1分同時受信機数**：その1分内の取得結果に含まれた現在受信中機数の最大値。
- **1時間/1日ユニーク機体数**：期間内に1回以上現在受信中となったユニークICAO数。
- **最遠受信**：有効位置を持つ観測のうち、受信地点からの大圏距離が最大のもの。
- **最接近**：有効位置を持つ観測のうち、受信地点からの大圏距離が最小のもの。

距離と方位は受信地点と航空機座標から計算する。異常座標、範囲外高度、未来時刻は除外し、除外件数を監視可能にする。

### 6.7 API

少なくとも以下を提供する。

- `GET /health/live`
  - Webプロセスが稼働しているか。
- `GET /health/ready`
  - DB接続と直近のreadsb取得状態を含む。
- `GET /api/status`
  - 最終成功取得、遅延、現在機数。
- `GET /api/traffic?hours=24`
  - 交通量の時系列。
- `GET /api/tracks?hours=6`
  - 最近の航跡。点数上限と間引きを設ける。
- `GET /api/rankings?hours=24&limit=10`
  - 最遠・最接近ランキング。
- `GET /api/aircraft/recent`
  - 最近観測した機体。

APIには入力値上限、ページングまたは点数制限、タイムアウトを設ける。

### 6.8 Web UI

1ページのダッシュボードから開始する。

- 上部：現在機数、位置取得中機数、最終更新、取得状態
- 中部左：過去24時間の交通量グラフ
- 中部右：最近の航跡地図
- 下部：最遠・最接近ランキング
- フッター：アプリバージョン、データ最終取得時刻

必要な表示要件：

- PCとスマートフォンで閲覧可能。
- データ停止中は古い数値を正常値のように表示しない。
- UTCで保存し、画面は指定タイムゾーンで表示する。
- 地図タイルが取得できない場合も、グラフとランキングは使える。
- 自宅位置を外部公開画面に精密表示しない。地図上では非表示または丸める設定を持つ。

### 6.9 保持と集計

- 詳細観測は既定30日で削除する。
- 1分集計は長期保存する。
- 削除処理は小さなバッチで行い、DBを長時間ロックしない。
- DBサイズ、最古・最新データ、1日当たり増加量を確認できる管理コマンドを用意する。
- 実測した1日当たり増加量から、30日後の容量を環境レポートへ追記する。

### 6.10 セキュリティ

- 初期状態では`127.0.0.1`またはDocker内部ネットワークだけにバインドする。
- LAN公開またはインターネット公開は明示的に選択する。
- 公開する場合はCaddy/Nginx等のリバースプロキシ、TLS、必要に応じて認証を使用する。
- PostgreSQLポートを外部公開しない。
- コンテナは可能な限り非rootユーザーで実行する。
- 書き込み可能なマウントを必要最小限にする。
- イメージタグを固定し、再現可能なビルドにする。
- Web画面/APIから任意URLを取得できるSSRF経路を作らない。

### 6.11 監視とログ

- 構造化ログを標準出力へ出す。
- 正常時に航空機ごとのログを毎回出さない。
- 最低限、以下を記録する。
  - 起動・終了
  - readsb取得失敗と回復
  - DB接続失敗と回復
  - 取得件数
  - 保存件数
  - 不正レコード除外件数
- Dockerヘルスチェックを実装する。
- 直近の取得成功が一定時間ない場合、`ready`を異常にする。

---

## 7. Phase 1 実装順序

### Task 1：リポジトリ初期化

- [ ] ディレクトリ構成を作成する。
- [ ] `README.md`、`.gitignore`、`.env.example`を作成する。
- [ ] Python依存関係を固定する。
- [ ] lint、format、testの実行方法を統一する。
- [ ] 実データを含まない匿名化fixtureを作成する。

### Task 2：Phase 0ツール

- [ ] 環境検査スクリプトを実装する。
- [ ] readsb probeを実装する。
- [ ] ローカルfixtureでもprobeをテストできるようにする。
- [ ] 対象サーバーで実行する。
- [ ] レポートを確認し、FAILがないことを確認する。

### Task 3：DBとマイグレーション

- [ ] PostgreSQLコンテナを定義する。
- [ ] 初期スキーマとインデックスを作成する。
- [ ] マイグレーションの適用・再適用試験を行う。
- [ ] バックアップと復元の最小手順を用意する。

### Task 4：collector

- [ ] HTTPクライアントと設定検証を実装する。
- [ ] readsbフィールドを内部モデルへ変換する。
- [ ] 距離と方位を計算する。
- [ ] staleデータを除外する。
- [ ] 位置点を間引いて保存する。
- [ ] 1分集計を生成する。
- [ ] 再試行、バックオフ、終了処理を実装する。

### Task 5：API

- [ ] health/status APIを実装する。
- [ ] traffic APIを実装する。
- [ ] tracks APIを実装する。
- [ ] rankings APIを実装する。
- [ ] 時間範囲と返却件数の上限を実装する。

### Task 6：Web UI

- [ ] ステータスカードを実装する。
- [ ] 24時間グラフを実装する。
- [ ] 航跡地図を実装する。
- [ ] ランキングを実装する。
- [ ] データ停止・API異常・地図異常の表示を実装する。
- [ ] スマートフォン表示を確認する。

### Task 7：保持処理と運用機能

- [ ] 詳細観測の保持期限処理を実装する。
- [ ] DBサイズ確認コマンドを実装する。
- [ ] バックアップ手順をREADMEへ記載する。
- [ ] ログローテーションまたはDockerログ上限を設定する。

### Task 8：テスト

- [ ] JSON欠損、null、文字列高度、`ground`、不正座標を単体テストする。
- [ ] stale判定をテストする。
- [ ] 距離・方位計算を既知の地点でテストする。
- [ ] 位置保存の間引きをテストする。
- [ ] readsb停止・復帰をテストする。
- [ ] DB停止・復帰をテストする。
- [ ] API上限と異常応答をテストする。
- [ ] Composeを使った結合テストを実行する。

### Task 9：サーバー配置

- [ ] Phase 0で確認した未使用ポートを採用する。
- [ ] `.env`をサーバー上だけに作成する。
- [ ] コンテナをビルドして起動する。
- [ ] localhostから画面とhealthを確認する。
- [ ] LAN/外部公開はユーザーの選択に従って設定する。
- [ ] OSまたはDocker再起動後の自動復旧を確認する。
- [ ] 既存サービスが継続稼働していることを確認する。

---

## 8. テストシナリオ

### 正常系

1. readsbが正常なJSONを返す。
2. collectorが取得し、DBへ保存する。
3. 1分後に交通量集計が生成される。
4. APIから現在値、時系列、航跡、ランキングを取得できる。
5. Web UIにデータが表示される。

### readsb停止

1. テスト用readsbモックを停止する。
2. collectorがクラッシュループせず、バックオフする。
3. UIが「データ取得停止」を表示する。
4. readsbモックを復旧する。
5. 自動的に取得が再開され、状態が正常へ戻る。

本番readsbサービスを停止して試験しない。障害試験にはモックまたは一時的に無効なテストURLを使用する。

### DB停止

1. テスト環境のDBだけを停止する。
2. メモリ使用量が無制限に増えないことを確認する。
3. `ready`が異常になることを確認する。
4. DB復旧後にcollector/APIが自動回復することを確認する。

### データ異常

- 空配列
- 一部フィールド欠損
- `lat`だけ存在
- `alt_baro: "ground"`
- 将来の時刻
- 範囲外座標
- 同じICAOの重複
- 非常に大きな航空機数

いずれもサービス全体が停止せず、対象レコードだけを安全に扱う。

---

## 9. Phase 1 最終受け入れ条件

- [ ] Linux x86-64サーバー上で起動する。
- [ ] readsb、tar1090、fr24feedに変更や停止を発生させない。
- [ ] 24時間以上連続稼働し、明らかなメモリ増加やクラッシュがない。
- [ ] 現在機数と最終更新時刻を表示できる。
- [ ] 24時間の交通量グラフを表示できる。
- [ ] 最近の航跡を地図表示できる。
- [ ] 最遠・最接近ランキングを表示できる。
- [ ] readsb停止時に異常を表示し、復旧時に自動回復する。
- [ ] 詳細観測の保持期限が機能する。
- [ ] DBが外部へ公開されていない。
- [ ] サーバー再起動後に自動復旧する。
- [ ] バックアップと復元の手順が文書化され、テスト環境で復元確認済みである。
- [ ] READMEだけで再配置・更新・停止・ログ確認ができる。

---

## 10. 今回の非対象

以下は将来フェーズとし、MVPへ入れない。

- Beast TCP生フレームの永続保存と独自Mode Sデコーダー
- 機種、航空会社、登録記号などの外部メタデータ統合
- 滑走路・到着・出発の推定
- ゴーアラウンドやホールディング検出
- 機械学習による異常検出
- ユーザーアカウント、複数受信局、一般公開サービス
- tar1090やfr24feedの置き換え
- Kubernetes

---

## 11. 実行前にユーザーから必要な情報

### 必須

1. **サーバーへの接続方法**
   - SSH先、ユーザー名、Codexが直接作業するか、コマンドをユーザーが実行するか。
2. **readsb JSONのURLまたはパス**
   - ブラウザーやサーバー内から開ける`aircraft.json`の場所。
3. **受信地点**
   - 緯度・経度、またはreadsbの`receiver.json`から取得してよいか。
4. **変更権限**
   - Docker導入などに必要なsudoが利用可能か。

### できれば事前に確認

5. Linuxディストリビューションとバージョン。
6. Docker/Composeがすでに導入済みか。
7. サーバーのメモリ、空きディスク。
8. 閲覧範囲：
   - 同じサーバー内だけ
   - 自宅LAN
   - インターネット公開
9. インターネット公開の場合、使用するドメイン、TLS、認証方式。
10. 希望する画面表示タイムゾーン。指定がなければ`Asia/Tokyo`。
11. 詳細航跡を30日保持してよいか。
12. 地図タイルに外部サービスを使用してよいか。

必須情報のうち、OS、Docker、リソースはPhase 0で自動確認できる。接続先と作業方法だけ先に確定すれば、環境テストの作成は開始できる。

---

## 12. 次の実行手順

1. ユーザーからサーバー接続方法とreadsb JSON取得先を受け取る。
2. ローカルでPhase 0ツールと匿名化fixtureを実装・テストする。
3. 対象サーバーでPhase 0を実行する。
4. 環境レポートを確認する。
5. FAILがなければPhase 1を実装する。
6. テスト環境で障害試験を行う。
7. 対象サーバーへ配置する。
8. 24時間の連続稼働確認後、Phase 1完了とする。

